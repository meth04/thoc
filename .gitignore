# Bí mật — không bao giờ commit
.env
.env.*
!.env.example

# Dữ liệu run (artifact tái tạo được từ seed/manifest; KHÔNG commit)
data/

# Scratch / tạm thời (log, pytest basetemp, script một-lần)
.tmp/
.agents/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/
.ruff_cache/

# IDE / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# docs/ và reports/ ĐƯỢC theo dõi (deliverable nghiên cứu: charter, ADR, review, báo cáo).
# Figure sinh từ analyze nằm trong data/runs/<run>/reports/ (đã bị data/ ignore).
