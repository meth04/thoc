# P0.1 — Capability registry + prompt-from-config (ADR 0006 §A, §B)

- Owner: `minds-engineer`. Ngày: 2026-07-13.
- Gate độc lập (CHƯA chạy — không phải tôi): `test-engineer`, `qa-verifier`, `adversarial-reviewer`.
- Đóng finding: F-01, F-02, F-03, F-04 (`docs/reviews/Report_v2-ledger.md`).
- **`catalog_hash()` hiện tại: `795188c725fc971f069ed01cd375888f756a8d12772500a2e874456f8e8d3ff4`**
  (38 descriptor, tất cả `cong_khai=True`).

## 1. Verdict

**PASS (technical-ready, chưa qua gate độc lập).** Baseline `308 passed, 1 skipped` được giữ
nguyên: **308 passed, 1 skipped**, `ruff` sạch. Không assertion nào bị nới/xóa/skip.

Ranh giới claim: đây là bằng chứng về **interface**, không phải về hành vi LLM, kết cục kinh tế
hay chuyên môn hóa. Prompt render đúng + menu đầy đủ **không** hàm ý agent sẽ làm nghề đò, khai
hoang hay phát minh tiền. `real60_spatial` giữ nhãn `diagnostic_only_unreplayable`.

## 2. Scope — file đã đổi

| File | Đổi gì |
|---|---|
| `minds/capabilities.py` | **MỚI**. 38 descriptor + `CATALOG` + `catalog_hash()` + `FIELD_KHONG_PHAI_ACTION` + `ap_dung_hanh_dong()` + `hanh_dong_tu_ke_hoach()` + `tai_san_giao_dich()` + `lich_mua()`/`mua_gieo_cay()`/`mua_kho()`/`cay_vu_dong()`. |
| `minds/schemas.py` | `LOAI_HANH_DONG` = `frozenset` DERIVED từ catalog (giữ tên public); sửa docstring "15 nguyên tố". |
| `minds/translate.py` | Bỏ hẳn chuỗi elif (≈200 dòng) → dispatch qua descriptor. **Thêm `dong_thuyen`/`rao_do`/`qua_song`** hai chiều. Giữ chữ ký `_mot_hanh_dong` (`minds/real.py:184` gọi trực tiếp). |
| `minds/prompts.py` | `LUAT_VAT_LY: str` → `luat_vat_ly(w)`; `MUC_HANH_DONG: list` → `muc_hanh_dong(w)`; `SCHEMA_QUYET_DINH` → `schema_quyet_dinh_cho(w)`; `[BẠN CÓ THỂ]` + `dat_lenh` asset list render từ catalog/world. |
| `minds/real.py` | **2 dòng** (`:202`, `:214`): `SCHEMA_QUYET_DINH` → `schema_quyet_dinh_cho(w)`. Không đụng logic transcript/gateway. |
| `tests/test_prompt_ky_luat.py` | **CHỈ import + dựng world** (hằng → hàm). Mọi assertion (`TU_MOM`, `TEN_DINH_CHE_CAM`, 8 ý định chính trị, căn tính giai cấp, việc làng) **giữ nguyên từng chữ**. |

KHÔNG đụng: `engine/**`, `run.py`, `minds/{transcript,gateway,rulebot,policies}.py`, `tools/**`,
`data/runs/**`, `reports/**`. KHÔNG thêm field vào `KeHoach`. KHÔNG thêm action ngoài 3 orphan.

## 3. Invariant

### CAP-1..4 (ADR 0006 §A.3) — code hỗ trợ test, số liệu đo được

```
so descriptor: 38 | cong_khai: 38
catalog_hash: 795188c725fc971f069ed01cd375888f756a8d12772500a2e874456f8e8d3ff4
LOAI_HANH_DONG == cac_ten_cong_khai(): True
CAP-2 field KeHoach mo coi: []
CAP-2 field khai bao khong ton tai: []
CAP-1 engine_handler thieu: []
CAP-1 from_kehoach loi: []
```

- **CAP-1**: mọi `engine_handler` import được bằng tên (`handler_ton_tai()`); `to_kehoach` +
  `from_kehoach` roundtrip. Hai action chia field `dat_lenh` (`dat_lenh`, `buon_chuyen`) khai báo
  `nguoi_phat_nguoc="dat_lenh"` — chiều ngược do một descriptor phát để **giữ thứ tự lệnh chợ**.
- **CAP-2**: `dataclasses.fields(KeHoach)` = 49 field. 47 field được descriptor khai báo;
  2 field trong `FIELD_KHONG_PHAI_ACTION` kèm lý do (`id`; `y_dinh_sinh_con` do policy-card).
  Thêm field mới mà quên khai báo ⇒ test FAIL (đây chính là cái đã bắt được 3 orphan).
- **CAP-3**: `menu == kha_dung(w)` HAI CHIỀU, đo trên base VÀ overlay spatial (§4).
- **CAP-4**: toàn bộ text render (menu + luật vật lý) đi qua bộ chặn `TU_MOM`
  (`nên `/`hãy`/`khôn ngoan`/`đáng`) và `TEN_DINH_CHE_CAM` (`ngân hàng`/`công ty`/`bảo hiểm`/
  `xưởng`) — pass cho cả hai config. Descriptor chỉ nêu **khả thi + chi phí + điều kiện + mã kết
  quả**; không xếp hạng sinh kế, không gợi ý action nào tốt hơn. Cơ chế xáo menu theo
  `w.rng.get(f"menu_xao:{aid}", w.tick)` **giữ nguyên** (`minds/prompts.py`, `build_agent_prompt`).

### `catalog_hash()` — reorder-invariant, interface-sensitive

```
catalog_hash goc       : 795188c7...8d3ff4
reorder                : 795188c7...8d3ff4  == goc: True
doi ham to_kehoach     : 795188c7...8d3ff4  == goc: True
doi mau_prompt_template: 0e5f5288...4dd4cd  != goc: True
bot 1 action           : ad73e4cc...68ee29  != goc: True
them schema_field      : 5014ba50...3161c4  != goc: True
doi kha_dung_key       : 8f4272e8...18e9c5  != goc: True
```

Payload băm = `sorted(CATALOG, key=ten)` × `{ten, kehoach_field, schema_fields, engine_handler,
kha_dung_key, mau_prompt_template, ma_ket_qua, thu_tu_phat, cong_khai, nguoi_phat_nguoc}`
(**superset** của ADR §A.2 — thêm `engine_handler`/`thu_tu_phat`/`cong_khai`/`nguoi_phat_nguoc`
vì chúng CŨNG là interface: `thu_tu_phat` quyết định thứ tự lệnh trên wire). KHÔNG băm file,
KHÔNG băm hàm ⇒ refactor thuần không đổi hash.

### PROMPT-1 (ADR 0006 §B.2) — parity đo thật

| Nội dung | Base (`config/world.yaml`) | Spatial (`+ scenarios/agrarian_transition_v1/spatial_v1.yaml`) |
|---|---|---|
| tick | `1 tick = 6 tháng; một năm = 2 tick` | `1 tick = 4 tháng; một năm = 3 tick` |
| vòng mùa | `lua → kho` | `lua_1 → lua_2 → dong` |
| khẩu phần | `90kg` / trẻ `45kg` | `60kg` / trẻ `30kg` |
| ngày công | `180 ngày công` | `120 ngày công` |
| giống/công/thửa | `60kg thóc giống + 60 công` | `40kg thóc giống + 40 công` |
| sản lượng | `~600kg × màu mỡ × thời tiết` ⚠ (xem F-14) | `~300kg × màu mỡ × thời tiết` |
| hao kho | `3%/tick` | `2.01%/tick` |
| homestead | `2 mùa lúa liên tiếp` | `4 mùa lúa liên tiếp` |
| nhà | `8 gỗ + 240 CÔNG` | `8 gỗ + 240 CÔNG` |
| đò/khai hoang/vụ đông | **KHÔNG** trong menu | **CÓ** (`dong_thuyen`, `rao_do`, `qua_song`, `khai_hoang`, `canh_vu_dong`, `cham_tre`) |
| asset `dat_lenh` | `ca\|cong_cu\|ga\|ga_con\|go\|may\|nha\|quang_dong\|thit\|thoc\|xu` | `+ khoai\|ngo\|thuyen` |

Không còn hằng số vật lý nào trong `minds/prompts.py`: mọi số đọc từ `w.cfg` (kể cả tuổi lao
động, gà, cá, đất bạc màu, tay nghề, tiệc, trộm, giáo dục, sinh nở, việc làng, ngưỡng Gini).

## 4. Evidence — lệnh + output THẬT (không mạng)

```
# (a) ngay sau khi P0.1 landed, TRƯỚC khi test-suite của P0.2 xuất hiện trong tree
$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m pytest -q --basetemp .tmp/p01 -p no:cacheprovider
308 passed, 1 skipped, 14 warnings in 152.55s (0:02:32)

# (b) trên tree hiện tại (P0.2 của engine-surgeon đã landed song song: +16 test journal)
$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m pytest -q --basetemp .tmp/p01d -p no:cacheprovider
324 passed, 1 skipped, 14 warnings in 156.14s (0:02:36)

$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m ruff check .
All checks passed!
```

Baseline trước P0.1: `308 passed, 1 skipped` ⇒ **không hồi quy, không test đỏ, không nới assertion**.
(Một lần chạy trung gian báo 3 FAIL trong `tests/test_resume_journal.py` — **transient**: file P0.2
đang được ghi trong lúc pytest chạy. Chạy lại trên tree ổn định: xanh. 3 test đó không đụng
capability/prompt/translate.)

### 4.1 CAP-3 hai chiều (script scratch, không commit)

```
### BASE: tick_moi_nam=2 mua(1..n)=['lua', 'kho']
kha_dung: 32 action (không có canh_vu_dong/cham_tre/dong_thuyen/rao_do/qua_song/khai_hoang)
CAP-3 menu == kha_dung: True
tai_san_giao_dich: ('ca','cong_cu','ga','ga_con','go','may','nha','quang_dong','thit','thoc','xu')
no-mutation world_hash: OK

### SPATIAL: tick_moi_nam=3 mua(1..n)=['lua_1','lua_2','dong']
kha_dung: 38 action (đủ đò/khai hoang/vụ đông/chăm trẻ)
CAP-3 menu == kha_dung: True
tai_san_giao_dich: (... 'khoai','ngo', ... 'thuyen', 'xu')
no-mutation world_hash: OK
```

### 4.2 Roundtrip 3 orphan + shape engine đọc

```
JSON phat ra: [{"loai":"canh_vu_dong","thua":"P05_06","cay":"ngo"},
 {"loai":"dong_thuyen","so_luong":2},
 {"loai":"rao_do","phi":5.0,"tai_san":"thoc"},
 {"loai":"qua_song","den_bo":"hoang","tai_san":"thoc","phi_chap_nhan":6.5},
 {"loai":"khai_hoang","thua":"P05_05"}]
ROUNDTRIP dong_thuyen/rao_do/qua_song/khai_hoang/canh_vu_dong: OK
THAM SO SAI -> thung intent la (khong raise): ['tham số sai: bờ lạ: mat_trang',
 "tham số sai: 'phi'", 'tham số sai: invalid literal for int()']
```

Shape khớp ĐÚNG cái `engine/spatial.py:112-190` `buoc_qua_song` đọc: `rao_do=(phi, tai_san)`;
`qua_song=(den_bo, tai_san_tra, phi_chap_nhan)` với `den_bo ∈ {"dan_cu","hoang"}`;
`dong_thuyen: int`. Tham số sai → **thùng intent lạ, không raise** (điều luật #3) — giữ nguyên.

### 4.3 Wire JSON byte-identical với bản CŨ (chống hồi quy `world_hash`)

So `ke_hoach_thanh_quyet_dinh` bản `git HEAD` vs bản mới trên một `KeHoach` dùng **mọi field
legacy** (kể cả `dat_lenh` XEN KẼ local/buôn-chuyến):

```
so hanh dong  CU: 43  MOI: 43
WIRE BYTE-IDENTICAL: OK
thu tu lenh cho: [('dat_lenh','go'), ('buon_chuyen','ga'), ('dat_lenh','go'), ('buon_chuyen','thit')]
THU TU LENH CHO GIU NGUYEN: OK
```

⇒ Đường mock (PersonaBot → `ke_hoach_thanh_quyet_dinh` → JSON → `quyet_dinh_thanh_ke_hoach`)
phát **cùng byte** cho mọi thế giới legacy. Cộng với: (a) rulebot/policy KHÔNG đi qua translate;
(b) `RngTree.get()` spawn Generator MỚI theo `(subsystem, tick)` (`engine/rng.py:26-31`) nên menu
dài ra không "ăn trộm" draw của stream khác ⇒ **`world_hash` của mọi run rulebot/mock legacy bất
biến**. (Toàn bộ test hash/determinism/replay legacy xanh.)

## 5. Findings còn mở (bàn giao)

| ID | Sev | Nội dung |
|---|---|---|
| **F-14** | **MAJOR — cần test-engineer đọc TRƯỚC khi viết parity test** | ADR 0006 §B.2 bảng parity ghi base ⇒ `650kg`. **`config/world.yaml:23` `san_luong_goc_kg: 600`.** `engine/production.py:255-264` dùng đúng `san_luong_goc_kg`. Vậy "~650kg" trong `LUAT_VAT_LY` cũ là **hằng số CHẾT ĐÃ DRIFT** — prompt nói dối agent 8% sản lượng ngay cả ở base. Tôi render **600** (sự thật config). **Test parity phải assert `600`, KHÔNG phải `650`.** Đây là bằng chứng bổ sung cho F-01 (hardcode luôn trôi). |
| **F-15** | MINOR | Hệ số tự-học ×2 (`engine/education.py:65` `so_tick * (2 if a.hoc_tu_hoc else 1)`) là **hằng số trong engine**, không có trong config. Prompt nêu "gấp đôi" là đúng engine, nhưng nó KHÔNG phải config-driven ⇒ property test "đổi khóa config ⇒ prompt đổi" không phủ được. Không sửa (ngoài scope: đụng `engine/`). |
| **F-16** | **INFO — có chủ đích, phải nêu to** | Mock spatial **đổi hành vi**: rulebot phát ý định đò cho **15/50** người; wire CŨ mang **0** action đò (nuốt êm), wire MỚI mang **20**. ⇒ `world_hash` của run **mock + `khong_gian.hai_bo=true`** SẼ ĐỔI. Đây là **fix F-02**, không phải hồi quy. Run base/legacy: `0 → 0`, hash bất biến. Mọi so sánh "LLM vs rulebot về đa dạng sinh kế" trên artifact CŨ vẫn **interface-confounded** và không được cứu bằng bản vá này. |
| **F-17** | INFO | `bao_huy` trước đây có trong `LOAI_HANH_DONG` + translate nhưng **KHÔNG có trong menu** (orphan cùng loại F-03, brief không nêu). Nay đã có dòng menu. Không thêm cơ chế engine. |
| **F-18** | INFO | `tai_san_giao_dich(w)` loại `cong`/`thuy_loi`/`dat` khỏi menu chợ (`TAI_SAN_KHONG_RAO`, có lý do từng mục). `co_phan:*` vào menu dưới dạng **văn phạm** `co_phan:<mã pháp nhân>` (không liệt kê từng entity — chống phình prompt). |

## 6. TODO cho parent (file tôi bị cấm đụng)

- `run.py` + `tools/experiments.py`: `from minds.capabilities import catalog_hash` →
  `reproducibility.capability_catalog_hash` trong `experiment_manifest.json` (ADR 0006 §A.2, P0.3).
- `tools/replay.py` + `tools/verify_research_run.py`: so `capability_catalog_hash` với manifest
  ⇒ `skipped_version_mismatch` = FAIL (ADR 0006 §C.4).
- `prompt_template_hash` (`run.py:59-64`, `sha256_file(minds/prompts.py)`) **ĐÃ ĐỔI** ⇒ transcript
  của `real60_spatial` **KHÔNG replay được** bằng code mới. Đúng như ADR §B.3 đã nói trước; không
  hứa cross-version replay.

## 7. Claim boundary

Bằng chứng ở đây thuộc cấp **`technical-ready`** (Report_v2 §8): interface đúng, catalog tự kiểm,
prompt khớp config đang chạy. **KHÔNG** phải `mechanism-ready`, `research-ready`,
`empirically-validated`. Sửa xong P0.1 không làm kết quả cũ đúng lên và không hứa kết quả mới đẹp
hơn; một kết quả âm sau khi sửa vẫn là kết quả hợp lệ.

---

# 8. Sửa sau adversarial gate (2026-07-13) — A-02 / A-15 / A-12

P0 gate FAIL: `adversarial-reviewer` + `reproducibility-steward` (độc lập) cùng chặn. Ba blocker
thuộc `minds/` đã sửa. **Không test nào bị nới** — `tests/test_prompt_config_parity.py` và
`tests/test_capability_parity.py` (test-engineer viết độc lập) giữ nguyên byte; code sai đã sửa.

## 8.1 A-02 — menu chào món mà GIẤU kinh tế học của nó (blocker khoa học)

**Bệnh.** `_gt_xay` cũ:

```python
mon = [f'"{m}"' for m in sorted(r) if m != "thuyen"]   # ⇒ QUẢNG CÁO cả "xu"
...
return {"mon": ..., "cong_cu": ..., "nha": ...}        # ⇒ chỉ CÔNG BỐ cong_cu + nha
```

Menu chào `{"loai":"xay","mon":"xu"}` nhưng prompt **chưa bao giờ** nói đúc xu tốn gì / ra bao
nhiêu (`config/world.yaml:32` → `xu: {quang_dong: 1, cong: 5, ra: 10}`; `engine/production.py:411-421`
thi hành). `duc_xu` là kênh **duy nhất** sinh ra `xu`. Vì vậy `reports/real60_evaluation.md` §2
(`đúc xu: 0 real / 1738 mock`) là **interface-confounded**: PersonaBot đọc `ctx`, LLM đọc prompt —
chỉ một trong hai từng được cho biết công thức. Con số 0 nói về INTERFACE, không nói về agent.

**Sửa.** Danh sách món chào **và** kinh tế học công bố nay rút từ MỘT nguồn — `cong_thuc_xay(w)`
(`minds/capabilities.py`) đọc thẳng config ⇒ không thể lệch nhau. Kèm bảng khai báo:
`MON_XAY` (món → field KeHoach + đường dẫn config recipe), `BI_DANH_MON_XAY` (`che_tac`→`cong_cu`),
`MON_NGOAI_XAY` (`thuyen` → action riêng `dong_thuyen`, kèm lý do),
`KHOA_RECIPE_KHONG_PHAI_NGUYEN_LIEU` (`ra`/`tang_nang_suat`/`hao_mon...`, kèm lý do),
`NHAN_NGUYEN_LIEU`, `DIEU_KIEN_MON_XAY`. Mọi khóa recipe KHÁC bị coi là nguyên liệu và **được nêu**
⇒ thêm nguyên liệu vào config thì prompt tự nói, không phải sửa code.

**Prompt THẬT sau khi sửa (base, render từ `config/world.yaml`):**

```
- {"loai":"xay","mon":"cong_cu"|"may"|"nha"|"xu"|"che_tac"|"<mã hàng mới>","so_luong":1}
   (chi phí đầu vào → sản phẩm đầu ra MỖI LẦN chế: cong_cu: 60 công + 2 gỗ → 1 cong_cu;
   may: 200 công + 8 gỗ + 6 quặng đồng (hoặc xu) → 1 may [cần blueprint cong_cu_may_moc —
   tự nghiên cứu ra hoặc được cấp quyen_su_dung]; nha: 240 công + 8 gỗ → 1 nha;
   xu: 5 công + 1 quặng đồng → 10 xu; che_tac: tên gọi khác của cong_cu (cùng công thức);
   <mã hàng mới>: hàng do một blueprint che_bien đẻ ra — công thức nằm trong CHÍNH blueprint đó,
   liệt kê ở khối BÍ QUYẾT BẠN NẮM trong phần riêng của bạn; không nắm blueprint nào thì không
   có mã hàng mới nào để chế)
```

**Trung lập (CAP-4 vẫn trùm CAP-5):** chỉ chi phí + sản phẩm. Không "xu tốt/xấu", không "nên đúc",
không chữ "tiền"/"tiền tệ". Bộ chặn từ-mớm/xếp-hạng/tên-định-chế chạy trên dòng mới: **KHÔNG CÓ**
từ cấm nào. `xu` nằm giữa `nha` và `che_tac` theo thứ tự chữ cái — không được ưu ái vị trí.

**Property (config-driven, không hằng chết):** đổi `recipe.xu` → `{quang_dong: 2, cong: 6, ra: 11}`
⇒ dòng menu đổi thành `xu: 6 công + 2 quặng đồng → 11 xu`. Hai test từng ĐỎ nay XANH:
`test_prompt1_doi_khoa_config_base_thi_prompt_doi[san_xuat.recipe.xu.cong]` và `[...xu.ra]`.

**`<mã hàng mới>`:** công thức KHÔNG nằm trong config (engine rút từ phân phối lúc sáng chế) ⇒ menu
chỉ ra nơi đọc được nó, và nơi đó nay **tồn tại thật**: khối mới `BÍ QUYẾT BẠN NẮM`
(`minds/prompts.py::_bi_quyet_cua`) — chỉ blueprint agent SỞ HỮU hoặc được cấp `quyen_su_dung`
hiệu lực (bất đối xứng thông tin giữ nguyên, chỉ đọc, không mutate):

```
BÍ QUYẾT BẠN NẮM (blueprint của riêng bạn / được cấp quyền dùng): BP0001 [che_bien, của bạn]
→ chế được mã hàng "tuong" bằng {"loai":"xay","mon":"tuong","so_luong":1}: 25 công + 1 gỗ + 3 thóc → 1 tuong.
```

**CAP-5 (bất biến mới, docstring `minds/capabilities.py`):** *không được quảng cáo một action —
hay một MÓN bên trong action — mà không công bố chi phí đầu vào + sản phẩm đầu ra, đọc từ config
đang chạy.* Hook máy kiểm cho test-engineer: `mon_recipe_khong_co_duong_che(w)` phải **rỗng**
(món có recipe mà không ở `MON_XAY` lẫn `MON_NGOAI_XAY` = config hứa một công thức không ai gọi
được). Đo thật: base `()`, spatial `()`.

## 8.2 A-15 — route nền thứ ba thiếu row transcript

`minds/real.py::_dich_intent_la` nhánh `except` chỉ ghi event `dich_intent_loi`, **không** gọi
`_ghi_call_loi` ⇒ replay gọi lại đúng call đó → `TranscriptProvider` **miss** → trượt cổng hard
`misses == 0` dù artifact sạch. Đã bịt. Quét hết `minds/real.py`: ba route nền
(`_nen_hoi_ky:177`, `_dich_intent_la:241`, `_reflection:329`) nay **đều** gọi `_ghi_call_loi`;
`_goi_nen_co_cho` và `GatewayCoPacing._cho_slot` chỉ re-raise (không nuốt).
`tests/test_p0_resume_independent.py::test_fp02_1b_route_dich_intent_hong_van_co_row_transcript`:
ĐỎ → XANH (16/16 passed).

## 8.3 A-12 — dead code phát biểu SAI SỰ THẬT (đã XÓA)

`mo_ta_the_gioi()` + `build_system()` (`minds/prompts.py:70-95`): `grep -rn` toàn repo
(`engine/`, `minds/`, `tools/`, `tests/`) → **không caller nào**. `build_system` nói *"Mọi trao đổi
tính bằng thóc"* — **SAI**: `engine.market.Lenh.thanh_toan` nhận mọi tài sản làm phương tiện thanh
toán, và menu `dat_lenh` nay nói đúng điều đó. Đã xóa cả hai; để lại comment ghi lý do.

## 8.4 Identity: `catalog_hash()` MỚI

```
catalog_hash() = a928fe2e612f7d0c143fbab89f5d61b205a66899eb6a05319fccda4c6eb7a553
```

Đổi là **ĐÚNG** (`mau_prompt_template` của `xay` đổi + bảng render vào hash). `catalog_hash()` nay
băm CẢ các **bảng render** (`MON_XAY`, `BI_DANH_MON_XAY`, `MON_NGOAI_XAY`,
`KHOA_RECIPE_KHONG_PHAI_NGUYEN_LIEU`, `NHAN_NGUYEN_LIEU`, `DIEU_KIEN_MON_XAY`,
`TAI_SAN_KHONG_RAO`, `FIELD_KHONG_PHAI_ACTION`) — chúng quyết định món nào được CHÀO và kinh tế học
nào được CÔNG BỐ, tức chính bề mặt agent nhìn thấy. Chỉ băm **khóa** với các bảng mà value là
free-text lý do (đổi lời giải thích ≠ đổi hành vi ⇒ hash không nhảy); băm **cả value** với
`NHAN_NGUYEN_LIEU`/`DIEU_KIEN_MON_XAY` vì value của chúng vào thẳng prompt.

## 8.5 Nghiệm thu (output THẬT)

```
$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m pytest -q --basetemp .tmp/me2 -p no:cacheprovider
477 passed, 1 skipped, 14 warnings in 205.53s (0:03:25)

$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m ruff check .
All checks passed!
```

**0 failed** (trước: 5 failed / 463 passed). Không mạng, không LLM, không `real`, không `.env`.

## 8.6 LỖ HỔNG CÒN LẠI — cho engine-surgeon (tôi bị cấm đụng `run.py`)

`prompt_template_hash` (`run.py::_repro_llm_meta:70-71`) = `sha256_file(minds/prompts.py)` **mà
thôi**. Nhưng bề mặt prompt nay có **THÂN HÀM** sống ở `minds/capabilities.py` (`_gt_xay`,
`_gt_phan_bo_cong`, …, `mo_ta_cong_thuc`) — chúng render text cho agent nhưng **không vào hash nào**:
`prompt_template_hash` chỉ băm `prompts.py`; `capability_catalog_hash` chỉ băm khai báo + bảng
render, KHÔNG băm thân hàm. ⇒ đổi công thức render (vd bỏ dấu `→`, đổi cách gộp nguyên liệu) sẽ đổi
prompt của MỌI agent trong khi **cả hai hash đứng yên** — hai run khác interface trông y hệt nhau
trong manifest. Vá đề xuất (một dòng, `run.py`): `prompt_template_hash` băm **cả**
`minds/prompts.py` **và** `minds/capabilities.py`.

---

# 9. F-CAP5-1..7 — `test-engineer` tổng quát hóa CAP-5, bắt 10 instance NỮA (2026-07-13)

`test-engineer` biến CAP-5 từ một test *về món `xay`* thành một **property đột biến** trên toàn
catalog (`tests/test_capability_parity.py::test_cap5_tham_so_kinh_te_hien_tren_be_mat_luat`): đổi
một tham số kinh tế học mà **engine cưỡng chế** ⇒ **BỀ MẶT LUẬT** (`schema_quyet_dinh_cho` +
`luat_vat_ly` + `build_user_chung`) **phải đổi theo**. Suite ĐỎ **10 failed / 543 passed**.

Đây đúng **lớp defect `duc_xu`**: engine cưỡng chế một con số mà prompt **không bao giờ công bố** ⇒
"agent không dùng cơ chế X" đo **INTERFACE**, không đo hành vi. Tôi sửa **CODE**, **không sửa test**
(test-engineer viết độc lập; chúng đỏ vì code sai). **Không đụng `engine/**`** — engine đúng, prompt
sai.

## 9.1 F-CAP5-1 (MAJOR) — **SỐ SAI**, không chỉ số bị giấu

Khác 6 cái còn lại: đây không phải "quên nói", mà là **prompt sẽ NÓI DỐI**.
`engine/chan_nuoi.py:96-106` rẽ nhánh theo cờ `_ga_rung_bat`:

| pool gà rừng | khóa engine ĐỌC | công thức |
|---|---|---|
| BẬT (spatial) | `khong_gian.ga_rung.cong_moi_con` | `so_con = (cong/dinh_muc) × mật_độ` |
| TẮT (base/legacy) | `chan_nuoi.bat_ga_cong_moi_con` | `so_con = floor(cong // dinh_muc)` |

`_gt_chan_nuoi` + `luat_vat_ly` cũ **luôn** render khóa legacy. Hôm nay **cả hai đều = 30** ⇒ prompt
đúng **do MAY MẮN**. Một ablation trên `cong_moi_con` (đúng cái ta muốn chạy để đo commons gà rừng)
sẽ khiến prompt nói một con số **engine không hề dùng**.

**Sửa**: `minds/capabilities.dinh_muc_bat_ga(w)` — **gương** của nhánh rẽ engine (cùng cờ
`_ga_rung_bat`), là nguồn duy nhất cho cả dòng menu `chan_nuoi` lẫn dòng CHĂN NUÔI trong
`luat_vat_ly`. Ablation chứng minh nhánh (script scratch, không commit):

```
scenario | ablation                           -> công/con prompt CÔNG BỐ
base    | (không)                            -> 30.0
base    | chan_nuoi.bat_ga_cong_moi_con=90   -> 90.0
spatial | (không)                            -> 30.0
spatial | khong_gian.ga_rung.cong_moi_con=90 -> 90.0
spatial | chan_nuoi.bat_ga_cong_moi_con=90   -> 30.0   ← engine KHÔNG đọc khóa này ở spatial
```

Dòng cuối là bằng chứng: prompt nay **im lặng đúng chỗ** thay vì lặp lại một con số chết.

## 9.2 F-CAP5-2 (MAJOR) — **giá của MỘT ĐIỂM nghiên cứu** (chạm câu hỏi nghiên cứu)

`engine/research.py:37`: `diem = (cong/cong_moi_diem + thoc/thoc_moi_diem) × he_so_E[E]`. Không có
hai con số này, agent **không tính nổi suất sinh lợi của R&D** ⇒ "agent không đầu tư nghiên cứu"
**không diễn giải được** — trong khi dự án hỏi thẳng liệu **phát minh** có tự phát sinh không. Menu
`nghien_cuu` nay công bố `cong_moi_diem`, `thoc_moi_diem` và `he_so_E` (đọc config sống). Tính đơn
điệu ("điểm càng nhiều thì xác suất mỗi tick ra blueprint càng cao, mức tăng nhỏ dần") là phát biểu
ĐÚNG về `p = 1 − exp(−điểm/(k0·(1+muc)^d))` (`research.py:99`); **tham số phân phối** (`k0`, `d`,
`he_so_roll_moi_tick`) vẫn **KHÔNG** công bố — đúng ranh giới `THAM_SO_PHAN_PHOI_KHONG_KHANG_DINH`
của test (quyết định của spec-governor, không phải của tôi).

## 9.3 F-CAP5-3..7 — cổng/chi phí bị giấu

| # | action | khóa engine cưỡng chế | nơi cưỡng chế | hệ quả khi giấu |
|---|---|---|---|---|
| 3 | `di_cu` | `di_cu.{so_thua_toi_thieu, cach_lang_toi_thieu, ban_kinh_cum}` | `engine/tick.py:352-354` | cổng khả thi CỨNG (`khong_du_cum_ruong`/`qua_gan_lang_cu`), **không con số nào** trong prompt |
| 4 | `cau_hon` | `nhan_khau.tuoi_truong_thanh` | `engine/demography.py:47,83` | gate `chua_du_tuoi` (áp cho **cả hai** bên) |
| 5 | `don_phuong_pha_vo` | `hop_dong.uy_tin.phat_vi_pham_mieng` | `engine/contracts.py:261` | chi phí **DUY NHẤT** của action ("mất uy tín") không có **độ lớn** ⇒ không so được với cái nhận từ phá vỡ |
| 6 | `de_nghi_hop_dong` | `hop_dong.de_nghi_het_han_tick` | `engine/board.py:97` | đề nghị biến mất lặng lẽ — trong khi `niem_yet` **đã** công bố hạn của nó (tiền lệ không nhất quán) |
| 7 | `nhan_tin` | `minds.p2p_hom_thu_toi_da` | `engine/tick.py:78` | cap **NHẬN** làm tin rơi IM LẶNG (`hom_thu_day`); cap **GỬI** thì đã công bố |

Với #5, prompt nêu cả `phat_vi_pham_mieng` (−0.5) lẫn phần lan tin đồn
(`× he_so_lan_tin_don` = −0.1) — cùng thang điểm với khối "THÂN QUEN & ÂN OÁN" agent vẫn đọc.

## 9.4 CAP-4 THẮNG CAP-5 (giữ nguyên)

Mọi text thêm vào là **DỮ KIỆN**: chi phí đầu vào + sản phẩm/hệ quả đầu ra + điều kiện, **đọc từ
config sống** (không một hằng số mới nào). Không xếp hạng sinh kế, không "nên/hãy/đáng", không tên
định chế, không nhãn giai cấp — `test_cap4_*` (bộ chặn `TU_MOM`/`TU_XEP_HANG`/`TEN_DINH_CHE_CAM`/
`NHAN_GIAI_CAP_CAM`, bản SAO ĐỘC LẬP) xanh trên **cả** base lẫn spatial.

## 9.5 Identity: `catalog_hash()` MỚI

```
catalog_hash() = 1926b8811298545b40d719bb7f206f83e19aff40208e916b7c93293df60a392f
```

(trước §9: `a928fe2e612f7d0c143fbab89f5d61b205a66899eb6a05319fccda4c6eb7a553`.) Đổi là **ĐÚNG**:
`mau_prompt_template` của `nghien_cuu`, `di_cu`, `chan_nuoi`, `cau_hon`, `don_phuong_pha_vo`,
`de_nghi_hop_dong`, `nhan_tin` đều đổi ⇒ **interface đổi** ⇒ hash phải nhảy. Run trước §9 và run sau
§9 **không so sánh được** như cùng một interface — đó chính là điều hash phải nói.

⚠ Lỗ hổng §8.6 **vẫn còn**: `dinh_muc_bat_ga`/`he_so_E_nghien_cuu`/`_gt_*` là **thân hàm** trong
`minds/capabilities.py`, **không vào hash nào** (`prompt_template_hash` chỉ băm `minds/prompts.py`).
Vá vẫn nằm ở `run.py` (tôi bị cấm đụng).

## 9.6 Nghiệm thu (output THẬT)

```
$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m pytest -q --basetemp .tmp/me4 -p no:cacheprovider
553 passed, 1 skipped, 14 warnings in 217.37s (0:03:37)

$ THOC_BLOCK_NETWORK=1 PYTHONIOENCODING=utf-8 PYTHONUTF8=1 conda run -n thoc-env \
    python -m ruff check .
All checks passed!
```

**0 failed** (trước: 10 failed / 543 passed). Không mạng, không LLM, không `real`, không `.env`,
không sửa `tests/**`, không sửa `engine/**`.

## 9.7 Claim boundary

- Đã sửa: 7 tham số kinh tế học **giờ hiện trên bề mặt luật**, và 1 trong số đó (F-CAP5-1) trước đây
  render **sai khóa**. Điều này **không** làm agent dùng các cơ chế đó — nó chỉ làm phép đo "agent
  không dùng X" **có nghĩa**.
- **Mọi run trước commit này** (kể cả `real60_*`) vẫn **interface-confounded** trên 7 cơ chế này,
  y như `duc_xu`: không được kết luận "agent không di cư / không nghiên cứu / không phá vỡ giao kèo"
  từ chúng.
- Lưới CAP-5 **chưa kín**: bảng `THAM_SO_KINH_TE` là bảng CỦA TEST, không suy ra tự động được từ
  code (mỗi `mau_prompt_gia_tri` là closure đục). Thêm một **khóa config mới cho action CŨ** vẫn lọt
  lưới. Đóng hẳn thì cần descriptor khai báo tường minh `khoa_kinh_te: tuple[str, ...]` — thay đổi
  API của registry, cần spec-governor duyệt.
