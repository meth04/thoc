# T02 + T03 — Gate độc lập (reproducibility-steward)

Ngày: 2026-07-12. Reviewer độc lập: reproducibility-steward (chỉ đọc/chạy read-only, KHÔNG viết
code này). Env: `conda run -n thoc-env`, git rev `d23b86c`, không real/API/mạng. Log: `.tmp/logs/`.

## T02 — PASS

- **G1 pytest**: **185 passed, 0 failed** (77s). Network guard: `THOC_BLOCK_NETWORK=1 pytest
  tests/test_providers.py` → 6 passed (guard không phá test).
- **G2 verify_research_run**: `agr_smoke_rb_s41` 8/8 PASS (replay hash `7232bec47f26` khớp, config
  digest reproduced). `review_manifest_smoke` replay hash khớp; config_digest = **WARN (soft)** do
  base config trôi — đúng thiết kế, không FAIL.
- **G3 replay --verify**: scenario `agr_smoke_rb_s41` TRÙNG; permute
  `review_cf_smoke_v1_c2_permute_personas_s31` TRÙNG (`replay.py` tái dựng overlay + treatment từ
  manifest — permute áp thật, không no-op).
- **G4 output isolation**: `analyze.py` ghi `data/runs/<run>/reports/`; `--global` copy sang
  `reports/final_analysis_<run>.md` (có hậu tố run) — KHÔNG đè shared. `compare.py` isolate tương tự.
- **G5 world_hash coverage (C12)**: ChinhQuyen (world.py:340-348), entities/blueprints/research
  (p4_s), quan hệ (qh_s), disease qua `a.health`; checkpoint `pickle.dump(self)` toàn-object +
  migrations. **ĐẠT.**

## T03 — PASS

- **G6 validation**: cả `preindustrial_closed_v1` + `agrarian_transition_v1` →
  `empirical_ready=false`, `safe_claim_label=mechanism_benchmark`, `provenance_all_sourced=false`,
  exit 0.
- **G7 scenario integrity**: `validation_tier=mechanism_benchmark`; priors toàn
  `design_assumption`; provenance source trống; targets rỗng có chủ đích; parameters chỉ override
  `chinh_tri.bat=false`. KHÔNG bịa địa danh/nguồn/DOI (các nhắc "DOI" chỉ là guardrail text).
- **G8 manifest**: `agr_smoke_rb_s41/experiment_manifest.json` có scenario_files_sha256,
  config_overlays, git_revision, config_sha256.

## Non-blocking (ghi nhận, không chặn)
1. pytest 185 (không phải ~190 như ước lượng); không có fail. → cập nhật con số thực.
2. `scenario_files_sha256` của `agr_smoke_rb_s41` pin 8/10 file: run tạo TRƯỚC khi thêm
   `data_contract.md` + `preanalysis_protocol.yaml`; run mới sẽ pin đủ. Chỉ là artefact thời điểm,
   không phải bug (2 file là doc/protocol, không ảnh hưởng trajectory).
3. Runner counterfactual: `_run_one` raise khi một run fail (abort ensemble) thay vì đếm failed —
   → T10 enhancement (graceful failed-run count).

**Verdict: T02 PASS, T03 PASS — 0 blocking.**
